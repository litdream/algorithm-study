#include <vector>
#include <iostream>
#include <algorithm>

std::vector<int>
merge(const std::vector<int>& left, const std::vector<int>& right);

std::vector<int>
merge_sort(const std::vector<int>& nums);
